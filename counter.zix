global make-counter (
	let count 0
	<= (
		set count $(add $count 1)
		get count
	)
)

global counter $(make-counter)

loop (
	let 'current count' $(counter)
	echo $'current count'
	'same strings?' false $('same strings?' $'current count' 10)
)
